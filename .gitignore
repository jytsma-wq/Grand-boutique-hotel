Nothing needs to be added to .gitignore since the only change is a modification to 'messages/en.json', which is a source/config file (.json). No build artifacts, dependencies, or temporary files are present in the changes.