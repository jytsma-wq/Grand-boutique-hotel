PS G:\Grand-boutique-hotel> npm run dev

> nextjs_tailwind_shadcn_ts@0.2.0 dev
> next dev -p 3000

'next' is not recognized as an internal or external command,
operable program or batch file.

✅ ISSUE RESOLVED:

Problem: 'next' is not recognized as an internal or external command

Solution: 
- Ran `npm install` to install all dependencies including Next.js CLI
- Verified installation with successful test run
- Development server now starts correctly on http://localhost:3000

The application is ready to run. Use `npm run dev` to start the development server.